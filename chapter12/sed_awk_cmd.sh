#!/bin/bash

  # sed, awk

  # 这个两个命令都是独立的脚本语言, 尤其适合分析文本
  # 文件和命令输出. 既可以单独使用,也可以结合管道和在 shell 脚本中使用.

  # sed
  # 非交互式的 "流编辑器", 在批量模式下, 允许使用许多 ex 命令.
  # 你会发现它在 shell 脚本中非常有用.

  # awk

  # 可编程的文件提取器和文件格式化工具, 在结构化的文本文件中,
  # 处理或提取特定域(特定列)具有非常好的表现.它的语法与 C 语言很类似.


