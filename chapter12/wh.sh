#!/bin/bash 

  #  which 

  #    which command-xxx 将会给出 "command-xxx" 的完整路径.
  #    当你想在系统中准确定位一个特定的命令或工具的时候, 这个命令就非常有用了.

       which rm

      #/usr/bin/rm

  #   whereis

  #    与上边的 which 很相似, whereis command-xxx 不只会给出 
  #    "command-xxx" 的完整路径,而且还会给出这个命令的 man 页 的完整路径.

  #   whatis

  #     whatis filexxx 将会在 whatis 数据库中查询 "filexxx". 
  #     当你想确认系统命令和重要的配置文件的时候, 这个命令就非常重要了. 
  #     可以把这个命令认为是一个简单的 man 命令

  
