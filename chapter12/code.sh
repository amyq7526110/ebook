#!/bin/bash
 
    #  uuencode

    #     这个工具用来把二进制文件编码成 ASCII 字符串,
    #     这个工具适用于编码 e-mail 消息体,或者新闻组消息.

    #  uudecode

    #     这个工具用来把 uuencode 后的 ASCLL 字符串恢复为二进制文件


    #   注意: fold -s 命令在处理从 Usenet 新闻组下载下来的长的 
    #   uudecode 文本消息的时候可能会有用(可能在管道中).


    # mimencode,mmencode 

     #  mimencode 和 mmencode 命令处理多媒体编码的 email 附件. 虽然 mail 用户代理
     #  (比如 pine 或 kmail) 通常情况下都会自动处理, 但是这些特定的工具允许从命令行或shell 脚本中来手动操作这些附件.

    #  crypt 

    #这个工具曾经是标准的 UNIX 文件加密工具. [3] 政府由于政策上的动机
    #规定禁止加密软件的输出, 这样导致了 crypt 命令从 UNIX 世界消失, 
    #并且在大多数的 Linux 发行版中也没有这个命令. 幸运的是, 程序员们想
    #出了一些替代它的方法, 在这些方法中有作者自己的 cruft (参见 Example A-4).



     













